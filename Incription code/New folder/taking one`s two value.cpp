/*#include <iostream>
#include <string>
using namespace std;

int main ()
{
	int i = 0;
	int z = 0;
	string message = "PAKISTANISBESTCOUNTRY";
	int len = message.length();
	len = len % 2;
	if ( len == 0)
	{
		cout <<"It is even no."<< endl;
	}
/*	cout << "here is the given lenght:" << len << endl; 
	for(; i < 2 ; i++)
	{
		char a = message[z];
		z++;
		cout << a << endl;
	}*/ 
#include <stdio.h>
#include <string>

/*int main ()
{
  char str[] = "This is a sample string";
  char * pch;
  printf ("Looking for the 's' character in \"%s\"...\n",str);
  pch=strchr(str,'s');
  while (pch!=NULL)
  {
    printf ("found at %d\n",pch-str+1);
    pch=strchr(pch+1,'s');
  }
  return 0;
}*/


using namespace std;

int main()
{
string messsage[] = "love is life";
int  phr = strchr(message,"s");
}
